---
description: Study this repo and build (or refresh) its committed memory — stack, commands, conventions, boundaries, and the always-on contracts — so every other skill reads it first. Replaces a manual onboarding pass; named onboard to avoid colliding with the built-in /init.
argument-hint: [optional focus area]
---
Use the **truestack-project-memory** skill to set up repo-local memory.

Focus: $ARGUMENTS (optional; default: full study pass)

Study the project from real evidence (manifests, config, scripts, tests, CI, layout) — never
invent — and create/refresh `CLAUDE.md` (under ~120 lines: summary, key commands, Principles,
Boundaries, Communication + Clarify + Honesty contracts, pointers) and `.ai/memory/`
(project-profile, architecture, lessons). Store only what the code can't tell you. Mark genuine
unknowns as TODO. Report in one plain line what you recorded.
